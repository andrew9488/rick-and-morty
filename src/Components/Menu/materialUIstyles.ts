import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles(() => ({
    root: {
        flexGrow: 1,
        maxWidth: 60,
        height: 96
    },
    tab: {
        width: 60,
        minWidth: 60
    }
}))